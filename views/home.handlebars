<div class="container">
    <div class="row">
        <div class="col-md-12">
            {{#each articles}}
            <div class="panel panel-primary">
                <div class="panel-heading">
                    <br>
                    <h4>{{this.title}}</h4>
                </div>
                <div class="panel-body">
                    <p> {{this.summary}}</p>
                    <a class="btn btn-primary" href={{this.url}} id="link" target="_blank">Click for Story!</a>
                    <button type="button" id="linkSave" class="btn btn-success save" data-id={{this._id}}>Save Article</button>
                    <br>
                    <br>
                </div>
            </div>
            <br>
            {{/each}}
        </div>
    </div>
</div>